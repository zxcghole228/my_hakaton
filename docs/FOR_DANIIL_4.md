# Уважаемому Даниилу: по Вашим трём правкам

1. **"Заменить описание скоринга"** — принято, формулировка была моя неточная:
   у Вас честная нормализация P(да)/(P(да)+P(нет)) из top_logprobs. Исправлено.

2. **"Сделать group split без товарной утечки"** — он таким был с первого дня:
   manual-сплит у нас групповой по компонентам связности (union-find), seed 42 —
   это сид выбора КОМПОНЕНТ, не пар. Пересечение товаров train/val = 0, проверено
   и записано в docs/FOR_DANIIL_2.md п.1. Ваша добавка при этом верна и внесена:
   из train-части LoRA дополнительно исключаются пары пилота и few-shot примеры.

3. **"Untouched teacher-test"** — сделан: labeling/teacher_test_untouched.parquet
   (25k, seed 777, ноль пересечений с пилотом по парам И по товарам). Пилот
   официально понижен до dev-set. Метрики экзамена расширены по Вашему списку
   (macro AP/AUC по категориям, FP по типам конфликтов, FN на перефразировках) —
   в labeling/TASK.md.

Мелкое: лимиты времени теста — 1/6/13 минут (Check/Public/Private), а не 20;
наши тайминги посчитаны против них с запасом ~2x (bge+TTA+стек тоже пройдёт).
OOF-скоринг для стека v4 — да, делаем (этот non-OOF дефект у нас уже был
пойман адверсариальной проверкой). Мягкий по-категорийный бленд против
жёсткого гейта — сравним оба на dev, итог на untouched.

Ваша лестница к 0.56 совпадает с нашей. Работаем.

С уважением, Егор и Клод.
