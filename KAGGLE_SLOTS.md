# План Kaggle-экспериментов: слоты на обсуждение

Логика: срочное и дешёвое — на остатки квот этой недели; тяжёлое — на субботний
сброс (сб ~3:00 МСК все получают свежие 30ч); корп-карта — только то, что T4
не тянет. Все эксперименты — на полигоне (data_polygon/llm_sample_2m.parquet),
валидация — общий llm-holdout, цифры сравнимы с таблицей solution/README.md.

## Пакет 1: препроцессинг CE (полигон + tiny, ~1-1.2ч за слот, всего ~5.5ч)

| Слот | Что проверяет |
|------|---------------|
| tiny_v1_160 | якорь — без него остальное не интерпретируется |
| tiny_v2_192 | формат Мишани: категория в тексте + атрибуты до 460 симв |
| tiny_v3_192 | "пересечение атрибутов вперёд" — под главный кластер ошибок (асимметрия карточек) |
| tiny_v1_224 | длина контекста 224 vs 160 |
| tiny_swap | swap-аугментация в чистом виде |

Кандидат: Егор (остаток 10ч20м, влезает с резервом). Результат блокирует
выбор формата текста для финального прогона — нужен раньше всего.

## Пакет 2: бэкбоны (полигон, тяжёлые слоты — на субботнюю квоту)

| Слот | Цена на T4 |
|------|-----------|
| SBE + tiny-якорь | ~2ч |
| ruBert-base | ~6-9ч |
| e5-base | ~7-10ч |
| bge-m3 (568M) | T4 не тянет — корп-карта/A100 или выкидываем |

Раздать: ruBert-base — один аккаунт, e5-base — другой (по одной 12ч сессии).

## Пакет 3: трюки обучения (полигон + tiny, ~1ч за слот, приоритет ниже)

| Слот | Что проверяет |
|------|---------------|
| tiny_confw | confidence weighting LLM-меток on/off |
| tiny_catbal | category-balanced loss on/off |

Частично подтверждено результатом e5-small — добивка на чьи-то остатки/субботу.

## Пакет 4: личная линия Мишани (боевой прогон, не полигон)

Фикс имён фэшн-категорий ("Галантерея и аксессуары", "Ювелирные изделия") +
дообучение e5-small до 2 полных эпох (~11ч) — самый вероятный следующий лучший
сабмит (прогноз LB ~0.49-0.50). С субботы, его аккаунт.

## Вне Kaggle (параллельно)

- Мак Егора (CPU): фичи/стек — features_v2, OOF-стекинг, абляции фичей.
- Корп-карта Дани: разметка (пилот -> боевые пары); bge-m3 если влезет.
- Сабмиты: 5/сутки на команду, согласование в чате.

## Как запускать (когда слоты разобраны)

Каждому нарезается персональный ноутбук из ablation_overnight.ipynb (матрица
EXPERIMENTS урезается до его слотов) — останется подключить датасет и Run All.
Результаты: ablation_results.json + строка в таблицу README + исполненный
ноутбук в notebooks/runs/.
