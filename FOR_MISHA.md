# Для Мишани: где мы, что работает, что брать

## Текущий лучший скор: LB 0.4500 (топ около 0.52)

Это **ce-v1 stage-A**: cross-encoder `cointegrated/rubert-tiny2` (29M параметров),
обученный 1 эпоху на всех 11.2M LLM-пар с soft labels (метки организаторов как
вероятности, BCE). БЕЗ дообучения на ручной разметке.

Ноутбук: `notebooks/ozon_ecup_crossencoder_v1.ipynb` (стадию B при повторе отключать,
см. ниже почему). Готовая модель у Егора: `ce_v1_ckpt.pt` (чекпойнт шага 40k).

## Главное знание, добытое за 2 дня (без него любой эксперимент мимо кассы)

1. **Тест ≈ LLM-распределение, а не ручное.** Обучение только на 365k ручных пар
   дает LB 0.283 при локальных 0.62 (проверено на catboost_v1 Дани). Обучение на
   11M LLM-пар — LB 0.37-0.45. Причина: в тесте новые товары, пары собраны так же,
   как LLM-пары.
2. **Валидация — только групповым сплитом** (union-find по товарам, товар не может
   быть и в train и в val). Случайный сплит по парам завышает метрику. Код сплита:
   `solution/train.py: group_split` (seed 42) — НЕ менять, иначе цифры несравнимы.
3. **Калибровка**: LB ≈ 0.6 × наш llm-holdout (macro). Проверено 5 сабмитами.
   Можно предсказывать LB не тратя попытку.
4. **Дообучение на ручных парах после LLM-претрейна ВРЕДИТ на LB**
   (A/B-тест: stage-A 0.450 vs stage-A+B 0.4355). На ручном holdout при этом
   растет (0.63→0.72) — не верь ручному holdout.
5. **Слабые категории у всех моделей одни**: Обувь, Одежда, Галантерея, Ювелирка
   (PR-AUC 0.2-0.4 против 0.6-0.9 у остальных). Дубль решает вариант исполнения
   (размер/цвет), модели это ловят плохо. Macro-метрика: подъем фэшна на +0.1
   дает +0.02 к итогу.

## Что уже в работе (не дублируй)

- **Стек GBM+CE** (Егор, локально): скоры cross-encoder'а фичей в CatBoost.
  Сабмит сегодня вечером/завтра утром.
- **tiny 2 эпохи stage-A** (Егор, Kaggle): доедет ~22:00 сегодня.
- **LLM-переразметка** (Даня, локальные модели): `labeling/TASK.md` + pilot_pairs.
- **rubert-base / e5-small stage-A** — СВОБОДНО, см. гипотезу №1.

## Гипотезы — бери любую, пиши в чат что взял

1. **Модель побольше (самый жирный кусок).** Тот же ноутбук, но
   `intfloat/multilingual-e5-small` (влезает в одну сессию Kaggle T4, ~9ч эпоха)
   или `rubert-base` (нужна A100 или резюме на 2-3 сессии). Ожидание по аналогии
   tiny→small на других задачах: +0.02-0.05 к LB. В ноутбуке поменять MODEL_NAME,
   EPOCHS_A/B = 1/0 (или 2/0), остальное как есть.
2. **Фичи фэшна для GBM**: нормализация размерных сеток (42-44 == 42, S == 44-46,
   детские 98-104), словарь синонимов цветов, парсинг размеров из name.
   Проверять на нашем сплите через `solution/train_v3.py`.
3. **Фичи Дани v2/v3 на LLM-распределении**: у него в ноутбуке TF-IDF (word+char),
   bi-encoder эмбеддинги, нормализация единиц — но обучал он на ручных парах.
   Перенести фичи в наш пайплайн (обучение на 11M LLM) — может дать +0.01-0.02.
4. **Порог длины/точности**: у нас max_len=160 токенов — проверить 224/256
   (фэшн-атрибуты длинные, может, режем важное). Цена: скорость падает линейно.
5. **Ансамбль сабмитов**: rank-average наших лучших csv (CE + GBM) прямо в
   run.py контейнера. Дешево, почти всегда +0.005-0.015.

## Правила гигиены

- Все локальные метрики — на сплитах из `solution/README.md`, иначе несравнимо.
- Лимит сабмитов: 5/сутки НА КОМАНДУ, согласовываем в чате перед отправкой.
- Что запустил/получил — коротко в чат + строку в таблицу README.
